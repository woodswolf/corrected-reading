<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LightTheme" parent="AppTheme">
        <item name="textColor">@color/black</item>
        <item name="bgColor">@color/white</item>
        <item name="cardBgColor">@color/white</item>
        <item name="gapBgColor">@color/light_gray</item>
    </style>

    <style name="DarkTheme" parent="AppTheme">
        <item name="textColor">@color/white</item>
        <item name="bgColor">@color/black</item>
        <item name="cardBgColor">@color/dark_gray</item>
        <item name="gapBgColor">@color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="LightThemeNoActionBar" parent="AppTheme">
        <item name="textColor">@color/black</item>
        <item name="bgColor">@color/white</item>
        <item name="cardBgColor">@color/white</item>
        <item name="gapBgColor">@color/light_gray</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DarkThemeNoActionBar" parent="AppTheme">
        <item name="textColor">@color/white</item>
        <item name="bgColor">@color/black</item>
        <item name="cardBgColor">@color/dark_gray</item>
        <item name="gapBgColor">@color/black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>